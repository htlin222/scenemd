# Optional Cloudflare API credentials for CI. Local deploy uses `wrangler login`.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CF_PAGES_PROJECT=scenemd
CF_PAGES_DIR=dist
CF_PAGES_BRANCH=main

# Stored in the scenemd-live Worker with `wrangler secret put HACKMD_API`.
HACKMD_API=
