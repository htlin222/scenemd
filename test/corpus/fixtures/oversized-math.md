# One derivation too tall

The derivation below is a single display-math block measured taller than the viewport capacity. Display math never splits.

$$
\begin{aligned}
OVERSIZED_{proof} &= \text{a derivation that renders taller than any scene} \\
&\quad \text{and therefore cannot be paginated by splitting}
\end{aligned}
$$

A closing paragraph after the math.
